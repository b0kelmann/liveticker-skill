# Demo video

Required by the GOSIM submission rules (3–5 min).

**Link:** _to be added before submission_

Suggested shot list (record on phone, no editing needed). Order matters —
the Auto-Broadcaster is the hero, lead with it.

1. **(0:00–0:30) Pitch.** "Live events lose their best moments. LiveTicker
   is an OpenClaw skill that makes the agent the co-host."
2. **(0:30–1:30) Auto-Broadcaster live demo.** Two participants post,
   one with `consent_public=true`. Run `python -m skill.loops.broadcaster`.
   Show the JSON the agent emits — the draft post, alt text, the audit
   line. Mention budget + consent gates briefly.
3. **(1:30–2:15) Bottleneck-Detector + Smart Helper.** Show the host view
   when a cluster of blockers appears. Show Smart Helper brokering an
   intro between two teams.
4. **(2:15–2:45) Why this is "Go Upstream".** Open `manifest.yaml`,
   point at the five capabilities; explain that other events plug in
   their own LLM / output skills, and improvements flow back upstream.
5. **(2:45–3:00) Roadmap + close.**
